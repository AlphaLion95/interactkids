import 'package:flutter/material.dart';

class PartsOfHouseScreen extends StatelessWidget {
  const PartsOfHouseScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Parts of House')),
      body: const Center(child: Text('Parts of House Module')),
    );
  }
}
