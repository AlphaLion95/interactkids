# This file makes the project in this directory runnable with `flutter run`.
# It simply points to the real app in the app/ subdirectory.

import 'app/main.dart' as app;

void main(List<String> args) {
  app.main();
}
